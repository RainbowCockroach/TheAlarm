package com.example.thealarm;

// Cho tính năng báo thức theo địa điểm
public class LocationBasedAlarm extends AlarmBase{

    // Để tạm cái constructor này ở đây cho hết lỗi, khi nào làm đến thì sửa sau
    public LocationBasedAlarm(boolean isActive, String name, byte accurateLevel) {
        super(isActive, name, accurateLevel);
    }
}
